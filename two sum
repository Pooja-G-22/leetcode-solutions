class Solution:
    def twoSum(self, nums, target):
        mp = {}

        for i in range(len(nums)):
            current = nums[i]
            need = target - current

            if need in mp:
                return [mp[need], i]

            mp[current] = i

        return []

        





